# 運用手順

正式文書として本拡張を使う際の運用手順です。読者は初めて運用する担当者を想定しています。

---

## 0. この文書の要点

- **Markdown は変更されません。** 変換は読み取りのみです。
- **保護には限界があります。** 編集制限は抑止、電子署名は検知です。改ざんの「防止」はできません。
- **閲覧環境を指定できない場合、実質的に機能する検知は SHA-256 の台帳照合だけです。**

---

## 1. 導入

### 1-1. 前提の確認

PDF 化にはレンダリングエンジンが必要です。以下のいずれかがインストールされていることを確認します。

- Google Chrome
- Microsoft Edge

標準的なインストール先は自動で検出されます。検出できない場合のみ、
設定 `markdownFormal.browser.executablePath` に実行ファイルの絶対パスを指定してください。

| OS | よくあるパス |
|---|---|
| macOS | `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome` |
| Windows | `C:\Program Files\Google\Chrome\Application\chrome.exe` |
| Linux | `/usr/bin/google-chrome` |

### 1-2. 拡張のインストール (社内配布)

VSIX ファイルを受け取ったら、以下を実行します。

```bash
# 拡張をインストールする
code --install-extension markdown-formal-pdf-0.2.1.vsix

# インストールされたことを確認する (一覧に表示されれば成功)
code --list-extensions | grep markdown-formal-pdf
```

> **注意**: VSIX 配布は自動更新されません。版が上がったら配り直す必要があります。

---

## 2. 文書の作り方

### 2-1. front matter を書く

Markdown の先頭に以下を記述します。表紙はここから自動生成されます。

```markdown
---
title: システム運用手順書
subtitle: 第 1.0 版
author: インフラ運用チーム
date: 2026-08-08
docno: DOC-INF-2026-0142
classification: 社外秘
---
```

| 項目 | 表紙での位置 | 省略 |
|---|---|---|
| `docno` | 左上 (文書管理番号) | 可 |
| `classification` | 右上 (赤いスタンプ) | 可 |
| `title` | 中央 | 可 |
| `subtitle` | 中央 | 可 |
| `author` / `date` | 中央下 | 可 |

### 2-2. 本文を書く

- `#` と `##` (見出し 1・2) の直前で改ページされます (設定 `heading.pageBreakLevel` で変更可)
- 見出しはページ末尾に単独で取り残されず、直後の内容と同じページに送られます
- 長い表・コードブロック・引用はページ境界で分割されます (行の途中では切れません)。
  PDF では続きのページに表の見出し行は繰り返されません。Word では繰り返されます
- 図はページをまたげないため、収まらない場合は次のページへ送られます
- 収まらない図表は `<div class="fit">` で囲むと縮小されます (PDF のみ)

---

## 3. 変換する

Markdown を開いた状態でコマンドパレット (`Ctrl/Cmd + Shift + P`) から実行します。

| コマンド | 出力 |
|---|---|
| `Markdown Formal: PDF に変換` | `.pdf` と `.pdf.sha256` |
| `Markdown Formal: Word (docx) に変換` | `.docx` と `.docx.sha256` |
| `Markdown Formal: PDF と Word の両方に変換` | 上記すべて |

出力先は Markdown と同じ場所です (設定 `outputDirectory` で変更可)。

**★コマンド名は VSCode の表示言語で変わります。** 本書は表示言語が日本語の場合の名称です。
英語表示のときは `Markdown Formal: Convert to PDF` / `Convert to Word (docx)` /
`Convert to both PDF and Word` になります。表示言語はコマンドパレットの
`表示言語を構成する` (`Configure Display Language`) から切り替えられます。

**★目次の見出しの既定値は英語の `Contents` です。** 日本語の文書を作る場合は、
設定 `markdownFormal.toc.title` に `目次` を指定してください。
署名理由の既定値 `Approved as an official document` も同様に
`markdownFormal.signing.reason` で変更できます。
これらは画面ではなく**出力される文書に載る**文字列のため、表示言語では切り替わりません。

---

## 4. 保護の設定

### 4-1. 方式を選ぶ

設定 `markdownFormal.protection.mode` で選びます。**併用はできません。**

| 値 | 内容 | 使いどころ |
|---|---|---|
| `none` | 保護しない | 下書き・回覧前 |
| `restrict` | 編集制限 (権限パスワード) | **証明書がまだ無い段階の既定** |
| `sign` | 電子署名 | 証明書が用意でき、閲覧環境も指定できる場合 |

### 4-2. 編集制限を使う場合

```
コマンドパレット → Markdown Formal: オーナーパスワードを保存 (SecretStorage)
```

パスワードは SecretStorage に保存され、`settings.json` には書かれません。

適用されたことは以下で確認できます。

```bash
# change:no と出れば編集が制限されている
pdfinfo 手順書.pdf | grep -i encrypted
```

> **限界**: 権限フラグはビューア任せの助言です。無視する閲覧ソフトが実在するため、
> うっかり編集の抑止にはなりますが改ざん防止にはなりません。

### 4-3. 電子署名を使う場合

**証明書の準備が先に必要です。**

1. 社内 CA で文書署名用の証明書 (`.p12` / `.pfx`) を発行する
2. 証明書を**リポジトリ外**の安全な場所に置く (例: `~/.certs/doc-signing.p12`)
3. 設定 `markdownFormal.signing.certificatePath` に絶対パスを指定する
4. `Markdown Formal: 証明書パスフレーズを保存 (SecretStorage)` を実行する
5. `markdownFormal.protection.mode` を `sign` にする

署名の確認:

```bash
# Signature is Valid. と出れば改ざんされていない
pdfsig 手順書.pdf
```

> **限界**: 署名は「防止」ではなく「検知」です。編集自体は可能で、
> 編集されると署名が無効になることで改ざんを判別します。

---

## 5. ★改ざん検知の運用 (最重要)

### 5-1. 閲覧環境による差

**署名の検証はビューアが行います。** そのため閲覧環境によっては検知されません。

| 閲覧環境 | 署名の検証 |
|---|---|
| Adobe Acrobat Reader | 自動で検証しバナー表示 (**推奨**) |
| Microsoft Edge | 機能はあるが**既定で無効**。`PDFSecureMode` ポリシー等で有効化が必要 |
| Google Chrome | **検証できない** (PDF 用の信頼ストアを持たない) |
| コマンドライン | `pdfsig` で検証可能 |

さらに Chrome には既知の不具合があり、**署名済み PDF を Chrome の「PDF に保存」で
印刷すると警告なく署名が消えます**。署名済み文書を Chrome で再保存しないでください。

### 5-2. 閲覧環境を指定できない場合

**SHA-256 の台帳照合を運用の柱にします。** これだけはビューアに依存しません。

**発行時**: 出力された `.sha256` の値を文書管理台帳に記録します。

```bash
# 出力と同時に作られる。中身をそのまま台帳に転記する
cat 手順書.pdf.sha256
# 例: efe17a83fcb591584962722169601665af890cae8a433dcb17b56c4c40f3ba7a  手順書.pdf
```

**受入時**: 受け取った側が照合します。

```bash
# OK と出れば改ざんされていない
shasum -a 256 -c 手順書.pdf.sha256

# 台帳の値と直接見比べる場合
shasum -a 256 手順書.pdf
```

Windows の場合:

```powershell
# 出力された値を台帳と見比べる
Get-FileHash -Algorithm SHA256 .\手順書.pdf
```

表紙の**文書管理番号**が台帳との紐付けキーになります。

### 5-3. 拡張からの検証

```
コマンドパレット → Markdown Formal: PDF を検証 (ハッシュ / 保護状態)
```

ページ数・編集制限の有無・署名の有無・SHA-256 が出力パネルに表示されます。

---

## 6. 段階移行の計画

| 時期 | 保護 | 検知 |
|---|---|---|
| 現在 (証明書なし) | 編集制限で抑止 | SHA-256 の台帳照合 |
| 証明書入手後 | 電子署名に切替 (編集制限とは排他) | 署名検証 + SHA-256 |

証明書が用意でき次第、`protection.mode` を `sign` に変えるだけで移行できます。

---

## 7. 困ったとき

| 症状 | 対処 |
|---|---|
| `Chrome または Edge が見つかりませんでした` | いずれかをインストールするか `browser.executablePath` を設定する |
| 目次のページ番号が出ない (Word) | Word で `Ctrl + A` → `F9` でフィールドを更新する |
| Windows / Linux で書体が違う | フォント設定に各 OS のフォールバックを並べる (既定はヒラギノ → 游 → Noto) |
| 表が右にはみ出す | 列数を減らすか、`<div class="fit">` で囲む (PDF のみ) |
| 分割された表の続きのページに見出し行が出ない (PDF) | 現状の制限です。Word 出力では繰り返されます |
| ページの下半分が大きく空く | 0.2.0 以前の不具合です。長いコードブロック・引用が丸ごと次ページへ送られていました。0.2.1 以降に更新してください |
| 署名が「未検証」と表示される | 自己署名証明書のため。社内 CA の証明書を各 PC の信頼ストアに配布する |
| 署名と編集制限を両方掛けたい | **できません。** 排他です。必要なら別ファイルとして出し分けてください |
